Python 3.9+
use the command to install dependancies 
pip install -r requirements.txt

Backend Fast API with Authentiation with Login, Register, Doctors Page 
still work in Progress to add more component.

Frontend React 